# v0.12.0 — MVPデモデータ投入・堅牢性改善・文書整備

日付: 2026-08-14

## 主な変更

### デモデータ（MVP / Prototype 確認用）
- `scripts/seed-demo.mjs` を追加（冪等・`--force` でデモ分のみ再投入可）
- 架空のデモユーザー 3 名（admin / user / viewer）・チーム 2 件・プロジェクト 4 件を投入
- デモ文献 16 件（論文・特許・Web・PDF。全て「【デモ用】」表記・`content_hash = demo-*`・架空 DOI/特許番号）
- 保存文献・検索履歴（ブックマーク付き）・AI 要約（レビュー状態含む）・比較表・レポート・ウォッチ・通知・監査ログ・LLM 使用量を投入
- 特許ファミリー画面が DB モードで動作するよう、同族候補（同一出願人・同分類）のデモ特許を追加

### 堅牢性
- 不正な UUID 等のパス指定で PostgreSQL 型変換エラーが 500 になっていたのを 404 に修正（`app.ts`）
- E2E スモークのエクスポート検証を「参考文献・出典」/「参考資料」の両形式対応に修正（AI モードでも PASS）

### スクリプト・文書
- `npm run seed:demo` / `npm run smoke:e2e` / `npm run smoke:local` を追加
- README にデモデータ手順、[docs/demo.md](../demo.md) にデモ確認手順を追加
- バージョンを 0.12.0 へ統一（package.json / package-lock.json / health API）

## テスト
- `npm run check` 全成功（typecheck / lint / test 123件（API 107・Web 16）/ build / audit）
- E2E スモーク（登録→検索→保存→要約→比較→レポート→エクスポート→ウォッチ→チャット）ALL PASSED
- ローカル systemd に反映済み（`/api/health` = 0.12.0・db ok）

## 影響範囲
- API: `apps/api/src/routes/health.ts`（バージョン表記）、`apps/api/src/app.ts`（エラー処理）
- Web: 変更なし（ビルド成果物は同一）
- スクリプト: `scripts/seed-demo.mjs`（新規）・`scripts/smoke-e2e.mjs`（検証条件修正）
- 文書: README・本ノート・`docs/demo.md`・運用台帳・state.json
