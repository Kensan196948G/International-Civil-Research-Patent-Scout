# v0.12.2 — MVP 専用環境・全項目ダミーデータ・検索バッチ化

日付: 2026-08-14

## 主な変更

### MVP / Prototype 環境（新設）
- サブドメイン: `https://icrps-mvp.mirai-dx-platform.com`
  - Cloudflare Worker `icrps-api-mvp`（custom domain）＋ 本番とは分離した Neon ブランチ（`icrps-mvp`）
  - Access 保護なし（関係者がデモアカウントで直接ログイン可能）
  - 本番 URL（`icrps.mirai-dx-platform.com`）は従来どおり Access 保護・変更なし
- Secrets: `DATABASE_URL`（MVP ブランチ）/ `JWT_SECRET` を MVP Worker に設定
- `wrangler.jsonc` に `env.mvp`、deploy workflow に mvp 環境（承認文字列 `approve-icrps-mvp`）を追加

### 全項目ダミーデータ（MVP DB に投入・保持）
- ユーザー 6 / チーム 3・メンバー 8 / プロジェクト 7（active・completed・archived）
- デモ文献 28（収集文献タブ用の情報源別デモ 12 件を含む。全て【デモ用】・架空 DOI/特許番号）
- 保存文献 22・検索履歴 5（ブックマーク・失敗含む）・検索結果 18・AI 要約 25（全種別・全レビュー状態）
- 比較表 3・レポート 5（全 5 テンプレート）・ウォッチ 5・通知 8・監査 27・LLM 使用量 12・認証トークン 2
- `npm run seed:demo`（`--force` 対応）で再現可能

### バグ修正（Cloudflare 固有）
- 検索実行時の 1 件ずつの DB 照会・INSERT が Workers のサブリクエスト上限（50/回）を超え 500 になっていた問題を、一括照会・一括 INSERT（`findDocumentsByContentHashes` / `insertDocumentsForSearch` / `insertSearchResultsBatch`）で解消

## テスト・検証
- `npm run check`: typecheck / lint / test 127（API 111・Web 16）/ build 全 PASS
- MVP API: `GET /api/health` = 0.12.2・env=mvp・db ok。横断検索 2.3 秒・21 件（OpenAlex 429 は部分失敗として表示）
- MVP 実ブラウザ E2E（scripts/ui-e2e.mjs）: 14/14 PASS・console error 0
- 本番 URL: Access 302 のまま（保護維持・変更なし）

## 影響範囲
- API: `repositories.ts`（検索バッチ化）/ `routes/search.ts` / `health.ts`
- デプロイ: `wrangler.jsonc`（env.mvp）/ `.github/workflows/deploy.yml`
- スクリプト: `scripts/seed-demo.mjs`（全項目カバー拡張）
- 文書: README / docs/demo.md / state.json / 運用台帳 / 本ノート
